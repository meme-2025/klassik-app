{
  "name": "klassik-contracts",
  "version": "1.0.0",
  "description": "Hardhat contracts for Klassik",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.2.0",
    "ethers": "^5.7.0"
  }
}