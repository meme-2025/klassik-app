# Frontend — Klassik

Ein einfaches statisches Frontend in `frontend/`.

Schnellstart:
- Öffne `frontend/index.html` in deinem Browser (oder nutze einen lokalen dev Server wie `http-server`).
- Die Wallet‑Connect Funktionalität nutzt `ethers.js` und MetaMask.
